server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/user_registration
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
